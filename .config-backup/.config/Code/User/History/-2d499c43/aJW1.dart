import 'package:flutter/material.dart';

class ListView extends StatelessWidget {
  final todo_list;
  const ListView({required this.todo_list, super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

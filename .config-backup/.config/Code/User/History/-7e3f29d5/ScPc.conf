# background @foreground

# --- Pywal integration ---
allow_remote_control yes

# Load pywal colors if they exist
include ~/.cache/wal/colors-kitty.conf

# --- Shell and startup ---
shell /bin/bash --login -c "fastfetch --config ~/.config/fastfetch/terminal_config; exec bash"